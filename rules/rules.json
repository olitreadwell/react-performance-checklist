[
  {
    "id": "R-01",
    "title": "Profile before you change code",
    "severity": "High",
    "category": "measure",
    "smell": false,
    "summary": "Record the slow moment, then read why each component rendered.",
    "why": "You cannot fix what you cannot see. Guessing wastes time.",
    "how": [
      "Record the slow action.",
      "If nothing feels slow, record normal use for a minute.",
      "At the top of the Profiler, one vertical bar per render. Taller means slower. That is the slowest commit.",
      "Click the tallest bar.",
      "In the flamegraph, horizontal bars. Wider means more time.",
      "Read why the component rendered."
    ],
    "bullets": [
      "Slow action: the thing the user says feels slow. Typing, opening a screen, applying a filter.",
      "If nothing feels slow: record normal use for a minute, then open the slowest render."
    ],
    "verify": "You can name the component and the reason.",
    "links": [
      {
        "kind": "guide",
        "label": "React DevTools Profiler guide",
        "url": "guides/react-dev-tools-profiler.md"
      }
    ]
  },
  {
    "id": "R-02",
    "title": "Render count is not the cost",
    "severity": "High",
    "category": "measure",
    "smell": false,
    "summary": "A re-render is a problem only if it takes time.",
    "why": "A small button rendered 5,000 times is cheap. A 10,000-row table rendered 5 times is expensive.",
    "how": [
      "Compare render time with the work inside.",
      "Find the cost, not the count."
    ],
    "bullets": [
      "Render time: how long one render takes.",
      "Work: what the component does during render. Sorting, filtering, building JSX.",
      "Count: how many times it renders.",
      "Cost: the time it actually takes."
    ],
    "verify": "You can quote a number, not a feeling.",
    "links": []
  },
  {
    "id": "R-03",
    "title": "Unstable props",
    "severity": "High",
    "category": "rendering",
    "smell": true,
    "smellText": "Unstable props into a memoized component",
    "summary": "A prop that is a new object every render defeats memo.",
    "why": "React compares identity, not content. A function or object written inline is a new object every render, so memo sees a change and cannot work.",
    "how": [
      "Hoist the value out of the component. Put it at the top of the file, or in the parent.",
      "Use useCallback for functions.",
      "Use useMemo for computed values.",
      "Or turn on React Compiler and let it do this for you."
    ],
    "bullets": [
      "What identity means: whether two values are the same object. Two objects with the same words are different objects. A function written inside a component is a new object every render. React compares identity, not content, so memo sees a change and cannot work.",
      "Smell: inline code in JSX."
    ],
    "note": "Looks bad but fine, leave it alone:\n\n- `onClick={() => ...}` passed to a component that is NOT memoized. No memo, no problem.\n- A string or number prop. Primitives compare by value, not identity.",
    "verify": "why-did-you-render goes quiet. React Scan shows no red flash there.",
    "links": [
      {
        "kind": "guide",
        "label": "why-did-you-render guide",
        "url": "guides/why-did-you-render.md"
      },
      {
        "kind": "docs",
        "label": "useCallback docs",
        "url": "https://react.dev/reference/react/useCallback"
      },
      {
        "kind": "docs",
        "label": "useMemo docs",
        "url": "https://react.dev/reference/react/useMemo"
      },
      {
        "kind": "docs",
        "label": "React Compiler docs",
        "url": "https://react.dev/learn/react-compiler"
      }
    ],
    "smellCode": "onClick={() => ...}\nstyle={{...}}",
    "example": "import { memo, useCallback } from \"react\";\n\nconst Row = memo(function Row({ onClick, style, items }) {\n  return (\n    <button onClick={onClick} style={style}>\n      {items.join(\", \")}\n    </button>\n  );\n});\n\n// Bad: a new function, a new object, and a new array every render.\n// memo sees a change every time, so it cannot help.\nfunction Parent() {\n  return (\n    <Row\n      onClick={() => save()}\n      style={{ color: \"red\" }}\n      items={[\"a\", \"b\"]}\n    />\n  );\n}\n\n// Good: hoist the values out. Same identity every render.\nconst RED = { color: \"red\" };\nconst ITEMS = [\"a\", \"b\"];\n\nfunction Parent() {\n  const save = useCallback(() => doSave(), []);\n  return <Row onClick={save} style={RED} items={ITEMS} />;\n}"
  },
  {
    "id": "R-04",
    "title": "Memo at the boundary",
    "severity": "Medium",
    "category": "rendering",
    "smell": false,
    "summary": "Wrap only expensive subtrees with changed props.",
    "why": "A subtree is a component and everything it renders. The boundary is the component you wrap with memo. Re-renders stop there.",
    "how": [
      "Good: memo on a list row that gets stable props.",
      "Bad: memo on a component whose parent passes inline props. The memo cannot help.",
      "Bad: memo on a component whose props change every render anyway."
    ],
    "bullets": [
      "Subtree: a component and everything it renders.",
      "Boundary: the component you wrap with memo. Re-renders stop at the boundary."
    ],
    "verify": "Render count drops where it matters, and the page still behaves.",
    "links": [
      {
        "kind": "docs",
        "label": "memo docs",
        "url": "https://react.dev/reference/react/memo"
      }
    ],
    "example": "import { memo } from \"react\";\n\n// Good: the row gets stable props. memo stops re-renders.\nconst Row = memo(function Row({ item }) {\n  return <li>{item.name}</li>;\n});\n\nfunction List({ items }) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <Row key={item.id} item={item} />\n      ))}\n    </ul>\n  );\n}\n\n// Bad: the parent passes an inline function.\n// The prop is new every render, so the memo cannot help.\nfunction List({ items }) {\n  return (\n    <ul>\n      {items.map((item) => (\n        <Row key={item.id} item={item} onClick={() => open(item)} />\n      ))}\n    </ul>\n  );\n}"
  },
  {
    "id": "R-05",
    "title": "Context re-renders every consumer",
    "severity": "Medium",
    "category": "rendering",
    "smell": true,
    "smellText": "One context holding many values",
    "summary": "One big context re-renders every consumer on any change.",
    "why": "A consumer is any component that reads the context. When the provider hands a new value, every consumer re-renders. The value is most often state, but context can carry anything: callbacks, config, theme constants. Context does not own data; it distributes whatever the provider hands it.",
    "how": [
      "Move state down to the component that uses it.",
      "Split one big context into smaller ones. Separate data from actions."
    ],
    "bullets": [
      "State and context are not the same. State is data a component owns and re-renders on change. Context is how a provider hands any value to every component beneath it without prop drilling.",
      "Consumer: any component that reads the context.",
      "What happens: when the context value changes, every consumer re-renders."
    ],
    "example": "import { createContext, useContext, useState } from \"react\";\n\n// Before: one context holds everything.\n// Any change re-renders all consumers.\nconst AppContext = createContext({ user, theme, save, logout });\n\n// After: two contexts.\n// A theme change re-renders only theme consumers.\nconst UserContext = createContext(null);\nconst ThemeContext = createContext(\"light\");\n\nfunction App() {\n  const [user, setUser] = useState(null);\n  const [theme, setTheme] = useState(\"light\");\n  return (\n    <UserContext.Provider value={{ user, setUser }}>\n      <ThemeContext.Provider value={{ theme, setTheme }}>\n        <Page />\n      </ThemeContext.Provider>\n    </UserContext.Provider>\n  );\n}\n\n// A consumer reads only what it needs.\nfunction ThemeButton() {\n  const theme = useContext(ThemeContext);\n  return <button className={theme}>Theme</button>;\n}",
    "verify": "In the Profiler, a sibling consumer no longer re-renders.",
    "links": [
      {
        "kind": "docs",
        "label": "Context docs",
        "url": "https://react.dev/learn/passing-data-deeply-with-context"
      }
    ],
    "smellCode": "const AppContext = createContext({ user, theme, save, logout });"
  },
  {
    "id": "R-06",
    "title": "Keys must be stable",
    "severity": "High",
    "category": "rendering",
    "smell": true,
    "smellText": "Index used as a key in a reordering list",
    "summary": "`key={index}` in a list that reorders or filters.",
    "why": "The index moves between items, so React rebuilds the wrong rows.",
    "how": [
      "Use the database id as the key.",
      "If there is no id, create one with crypto.randomUUID() when the item is created.",
      "Signs of an unexpected remount: the input loses focus, a checkbox unchecks, the scroll jumps, the Profiler shows a new mount."
    ],
    "bullets": [
      "Unexpected remount: the component loses its state. Signs: the input loses focus while you type, a checkbox unchecks, the scroll jumps, the Profiler shows a new mount.",
      "How to check: type in a row's input, then reorder the list. If the input loses focus, the row remounted."
    ],
    "example": "import { useState } from \"react\";\n\n// Give each item an id once, when it is created.\nconst [items, setItems] = useState(() => [\n  { id: crypto.randomUUID(), text: \"first\" },\n  { id: crypto.randomUUID(), text: \"second\" },\n]);\n\n// Add new items the same way.\nsetItems((prev) => [...prev, { id: crypto.randomUUID(), text: \"new\" }]);\n\n// Bad: the index moves between items.\n// React rebuilds the wrong rows and state resets.\n{items.map((item, index) => <Row key={index} item={item} />)}\n\n// Good: the id stays with the item.\n{items.map((item) => <Row key={item.id} item={item} />)}",
    "verify": "No remounts in the Profiler after a reorder.",
    "links": [
      {
        "kind": "docs",
        "label": "Rendering lists docs",
        "url": "https://react.dev/learn/rendering-lists"
      }
    ],
    "smellCode": "items.map((item, index) => <Row key={index} item={item} />)"
  },
  {
    "id": "R-07",
    "title": "Heavy work in render",
    "severity": "High",
    "category": "rendering",
    "smell": true,
    "smellText": "Sorting or filtering inside the render body",
    "summary": "Sorting, filtering, formatting, or parsing during render.",
    "why": "Code in the component body runs on every render. Look for .filter(), .sort(), .map() over big arrays, JSON.parse, date formatting.",
    "how": [
      "Move the work into useMemo if the result repeats for the same inputs.",
      "Move it into an event handler, an effect, or a web worker if it runs once.",
      "Move it into the build step if it never changes.",
      "For long lists, render only the visible rows with react-window (virtualization)."
    ],
    "bullets": [
      "Heavy work: sorting, filtering, formatting, or parsing during render.",
      "How to tell: code in the component body runs on every render. Look for `.filter(`, `.sort(`, `.map(` over big arrays, `JSON.parse`, date formatting.",
      "Where to start: the component that owns the list (the parent of the rows). Then follow the widest bars in the flamegraph.",
      "Long lists: [virtualization](glossary.md), render only the visible rows with [react-window](https://react-window.vercel.app/)."
    ],
    "verify": "The flamegraph gets shorter. Typing stays smooth.",
    "links": [
      {
        "kind": "docs",
        "label": "react-window",
        "url": "https://react-window.vercel.app/"
      },
      {
        "kind": "docs",
        "label": "TanStack Virtual",
        "url": "https://tanstack.com/virtual"
      }
    ],
    "smellCode": "const sorted = items.sort((a, b) => a - b); // runs on every render",
    "example": "import { useMemo } from \"react\";\n\n// Bad: the sort runs on every render.\nfunction List({ items }) {\n  const sorted = items.sort((a, b) => a.price - b.price);\n  return (\n    <ul>\n      {sorted.map((item) => (\n        <li key={item.id}>{item.name}</li>\n      ))}\n    </ul>\n  );\n}\n\n// Good: the sort runs only when items change identity.\n// The map below still runs on every render. That is the cheap part.\nfunction List({ items }) {\n  const sorted = useMemo(\n    () => [...items].sort((a, b) => a.price - b.price),\n    [items]\n  );\n  return (\n    <ul>\n      {sorted.map((item) => (\n        <li key={item.id}>{item.name}</li>\n      ))}\n    </ul>\n  );\n}",
    "note": "useMemo does not skip the map. The map still runs on every render, and that is the cheap part: it only builds element objects. useMemo skips the sort, and only when items keeps the same identity. If the parent builds the array inline (a new array every render), the sort runs anyway. If the rows themselves are expensive to re-render, memoize the row at the boundary (see item 4). useMemo does not stop re-renders, it only caches the value."
  },
  {
    "id": "R-08",
    "title": "Inline components",
    "severity": "High",
    "category": "rendering",
    "smell": true,
    "smellText": "Component defined inside another component",
    "summary": "A component defined inside a component unmounts its children on every render.",
    "why": "Each render creates a new component type. React sees a different type, throws away the old subtree, and builds a new one. State resets and the page re-renders far more than needed.",
    "how": [
      "Define components at module scope, never inside another component.",
      "If a component needs props, pass them. Do not close over local values by defining it inline."
    ],
    "bullets": [
      "Smell: a `function`, `const`, or arrow component declared inside the render body of another component."
    ],
    "note": "The fix is not memo. The fix is moving the component out of the render body.",
    "verify": "The Profiler shows no full remounts on parent re-renders.",
    "links": [
      {
        "kind": "docs",
        "label": "React docs: don't define components inside components",
        "url": "https://react.dev/learn/your-first-component#defining-and-using-a-component"
      }
    ],
    "smellCode": "function Parent() {\n  const Inner = () => <span />;\n  return <Inner />;\n}",
    "example": "// Bad: Inner is a new component type every render.\n// React unmounts and remounts it each time.\nfunction Parent() {\n  const Inner = () => <span>hi</span>;\n  return <Inner />;\n}\n\n// Good: Inner lives at module scope. Same type every render.\nfunction Inner() {\n  return <span>hi</span>;\n}\n\nfunction Parent() {\n  return <Inner />;\n}"
  },
  {
    "id": "R-09",
    "title": "Debounce high-frequency input",
    "severity": "Medium",
    "category": "rendering",
    "smell": false,
    "summary": "Every keystroke or scroll event triggers a state update or network call.",
    "why": "Each state update triggers a render. Typing, scrolling, and resizing fire dozens of events per second, so raw updates make the page janky.",
    "how": [
      "Debounce search input: wait 150-300 ms after the last keystroke before updating.",
      "Throttle scroll and resize handlers so they run at most once per frame.",
      "Add { passive: true } to scroll, wheel, and touch listeners.",
      "Use requestIdleCallback to defer work that is not urgent."
    ],
    "bullets": [
      "Debounce: run the update after a pause in events. [Definition](glossary.md)",
      "Throttle: run the update at most once per time window. [Definition](glossary.md)"
    ],
    "verify": "The Profiler shows one render per pause, not one per event.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev: debounce your input handlers",
        "url": "https://web.dev/articles/optimize-inp#debounce-input-handlers"
      },
      {
        "kind": "docs",
        "label": "Vercel: use passive event listeners",
        "url": "https://github.com/vercel-labs/agent-skills/tree/main/skills/react-best-practices/rules/client-passive-event-listeners.md"
      }
    ]
  },
  {
    "id": "L-01",
    "title": "Split the code",
    "severity": "High",
    "category": "load",
    "smell": true,
    "smellText": "All routes in one bundle",
    "summary": "All routes in one bundle.",
    "why": "Code splitting loads code only when a route needs it. The first load is slow when everything ships at once.",
    "how": [
      "Use React.lazy and Suspense around routes.",
      "Split at the route level, not every component.",
      "If a chunk is small and rarely changes, leave it in the main bundle."
    ],
    "bullets": [
      "Code splitting: loading code only when a route needs it. [Definition](glossary.md)",
      "How to know you need it: the first load is slow, the bundle is big, or a heavy screen is rarely used.",
      "How to know you went overboard: too many tiny chunks means too many network requests. Split at the route level, not every component. If a chunk is small and rarely changes, leave it in the main bundle."
    ],
    "example": "import { lazy, Suspense } from \"react\";\n\n// Load a screen only when the user opens it.\nconst Settings = lazy(() => import(\"./Settings\"));\nconst Profile = lazy(() => import(\"./Profile\"));\n\nfunction App() {\n  return (\n    <Suspense fallback={<p>Loading...</p>}>\n      <Settings />\n      <Profile />\n    </Suspense>\n  );\n}",
    "verify": "The first load downloads fewer bytes.",
    "links": [
      {
        "kind": "docs",
        "label": "React.lazy docs",
        "url": "https://react.dev/reference/react/lazy"
      }
    ],
    "smellCode": "import Settings from \"./Settings\";\nimport Profile from \"./Profile\"; // both load on first visit"
  },
  {
    "id": "L-02",
    "title": "Shrink the bundle",
    "severity": "Medium",
    "category": "load",
    "smell": false,
    "summary": "The bundle is bigger than the app needs.",
    "why": "Tree-shaking removes code you never use. Big packages you barely use cost every user download time.",
    "how": [
      "Check with vite-bundle-visualizer or webpack-bundle-analyzer.",
      "Remove or replace big packages.",
      "Use per-package imports."
    ],
    "bullets": [
      "Tree-shaking: the build removes code you never use. [Definition](glossary.md)"
    ],
    "verify": "The bundle picture gets smaller.",
    "links": [
      {
        "kind": "guide",
        "label": "Bundle visualizer guide",
        "url": "guides/bundle-visualizer.md"
      },
      {
        "kind": "docs",
        "label": "vite-bundle-visualizer",
        "url": "https://www.npmjs.com/package/vite-bundle-visualizer"
      },
      {
        "kind": "docs",
        "label": "webpack-bundle-analyzer",
        "url": "https://www.npmjs.com/package/webpack-bundle-analyzer"
      }
    ],
    "example": "// Bad: imports the whole package.\nimport _ from \"lodash\";\n\n// Good: imports only the function you use.\nimport debounce from \"lodash/debounce\";",
    "exampleLang": "js"
  },
  {
    "id": "L-03",
    "title": "Images and fonts",
    "severity": "Medium",
    "category": "load",
    "smell": true,
    "smellText": "Images without lazy loading",
    "summary": "Images without lazy loading, or critical fonts loading late.",
    "why": "Lazy loading means the image loads only when it is near the screen. Large images and late fonts slow LCP and cause layout shift.",
    "how": [
      "Add loading=\"lazy\" to images below the fold.",
      "Add sizes.",
      "Preload the critical font."
    ],
    "bullets": [
      "Lazy loading: the image loads only when it is near the screen. [Definition](glossary.md)"
    ],
    "verify": "LCP and CLS improve in Lighthouse.",
    "links": [
      {
        "kind": "guide",
        "label": "Lighthouse guide",
        "url": "guides/lighthouse.md"
      },
      {
        "kind": "docs",
        "label": "react-intersection-observer",
        "url": "https://github.com/thebuilder/react-intersection-observer"
      }
    ],
    "smellCode": "<img src=\"hero.jpg\" />",
    "example": "// Bad: every image loads on page open.\n<img src=\"hero.jpg\" />\n\n// Good: below-the-fold images load only when near the screen.\n<img\n  src=\"hero.jpg\"\n  loading=\"lazy\"\n  sizes=\"(max-width: 600px) 100vw, 50vw\"\n/>"
  },
  {
    "id": "L-04",
    "title": "Preload and preconnect",
    "severity": "Medium",
    "category": "load",
    "smell": false,
    "summary": "Critical resources discovered too late, or connections opened only when needed.",
    "why": "Preload starts the download before the code asks for the resource. Preconnect opens the connection early. Both shorten the critical path.",
    "how": [
      "Add `<link rel=\"preload\">` for the resource that becomes LCP; the image, font, or script the first screen needs.",
      "Add `<link rel=\"preconnect\">` to the origins the page needs early, like a font CDN.",
      "Prefetch links the user will likely click next."
    ],
    "bullets": [
      "Preload: download a critical resource early. [Definition](glossary.md)",
      "Preconnect: open a connection to an origin before it is needed. [Definition](glossary.md)"
    ],
    "verify": "Lighthouse resource-hint audits pass, and LCP improves.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev: preload and preconnect",
        "url": "https://web.dev/articles/uses-rel-preload"
      }
    ],
    "example": "<!-- Bad: the LCP image is discovered only when the page parses it. -->\n<img src=\"hero.webp\" />\n\n<!-- Good: the download starts before the parser reaches it. -->\n<link rel=\"preload\" as=\"image\" href=\"hero.webp\" />\n<img src=\"hero.webp\" />"
  },
  {
    "id": "L-05",
    "title": "Barrel imports",
    "severity": "Medium",
    "category": "load",
    "smell": true,
    "smellText": "Importing from a barrel file instead of the module",
    "summary": "Importing from a barrel file can load whole libraries you never use.",
    "why": "A barrel file re-exports many modules from one index. Importing from it makes the bundler pull in modules you never use. Icon and component libraries can re-export thousands of modules, adding hundreds of milliseconds to the import and bloating the bundle.",
    "how": [
      "Import from the exact module path, not the barrel index.",
      "For large libraries, use subpath imports or the bundler's optimizePackageImports.",
      "Check the bundle visualizer for whole libraries appearing on one import."
    ],
    "bullets": [
      "Barrel file: an index that re-exports many modules. [Definition](glossary.md)"
    ],
    "verify": "The bundle visualizer no longer shows the whole library for one import.",
    "links": [
      {
        "kind": "docs",
        "label": "Vercel: avoid barrel file imports",
        "url": "https://github.com/vercel-labs/agent-skills/tree/main/skills/react-best-practices/rules/bundle-barrel-imports.md"
      }
    ],
    "smellCode": "import { Button } from \"@ui-kit\";",
    "example": "// Bad: the barrel index re-exports every module in the package.\nimport { Button } from \"@ui-kit\";\n\n// Good: the bundler loads only the Button module.\nimport { Button } from \"@ui-kit/button\";",
    "exampleLang": "js"
  },
  {
    "id": "D-01",
    "title": "Parallel independent fetches",
    "severity": "High",
    "category": "network",
    "smell": true,
    "smellText": "Sequential awaits that could run in parallel",
    "summary": "Independent requests that run one after another add full network latency per request.",
    "why": "A waterfall means every sequential await adds a full round trip. If three fetches do not depend on each other, running them one at a time takes three times longer than running them together.",
    "how": [
      "Run independent fetches with Promise.all.",
      "Check cheap synchronous conditions before awaiting (see D-03).",
      "Run partial dependencies by starting the shared promise early and awaiting it in each branch.",
      "Check cheap synchronous conditions before awaiting a remote value."
    ],
    "bullets": [
      "Waterfall: requests that run one after another because each waits for the one before. [Definition](glossary.md)"
    ],
    "verify": "The network tab shows the fetches overlapping, and total wait drops to the slowest one.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev: fetch waterfalls",
        "url": "https://web.dev/articles/fetch-waterfalls"
      },
      {
        "kind": "docs",
        "label": "Vercel: react-best-practices async rules",
        "url": "https://github.com/vercel-labs/agent-skills/tree/main/skills/react-best-practices"
      }
    ],
    "smellCode": "const user = await fetchUser();\nconst cart = await fetchCart();",
    "example": "// Bad: three round trips, one after another.\nconst user = await fetchUser();\nconst cart = await fetchCart();\nconst posts = await fetchPosts();\n\n// Good: one round trip worth of waiting.\nconst [user, cart, posts] = await Promise.all([\n  fetchUser(),\n  fetchCart(),\n  fetchPosts(),\n]);"
  },
  {
    "id": "D-02",
    "title": "Cache and dedupe data fetching",
    "severity": "Medium",
    "category": "network",
    "smell": false,
    "summary": "The same request fires again and again, or every mount refetches.",
    "why": "Duplicate requests waste bandwidth and make the UI flash between loading states. A cache keeps one in-flight request per key and reuses the result.",
    "how": [
      "Use TanStack Query or SWR. They dedupe in-flight requests, cache responses, and refetch on your schedule.",
      "Set staleTime so fresh data is not refetched on every mount.",
      "If you fetch by hand, cache the promise, not just the result, so concurrent callers share one request."
    ],
    "bullets": [
      "Dedupe: two callers of the same key share one request. [Definition](glossary.md)",
      "staleTime: how long cached data counts as fresh. [Definition](glossary.md)"
    ],
    "verify": "The network tab shows one request for N callers.",
    "links": [
      {
        "kind": "docs",
        "label": "TanStack Query",
        "url": "https://tanstack.com/query"
      },
      {
        "kind": "docs",
        "label": "SWR",
        "url": "https://swr.vercel.app/"
      }
    ]
  },
  {
    "id": "D-03",
    "title": "Check cheap conditions before await",
    "severity": "Medium",
    "category": "network",
    "smell": true,
    "smellText": "Awaiting a remote value before checking a cheap local condition",
    "summary": "An await can run a full network round trip even when a cheap local check already fails.",
    "why": "`const flag = await getFlag(); if (flag && localCheck)` always pays for the async call. `if (localCheck) { const flag = await getFlag(); ... }` skips the call entirely when the local check fails. On the cold path that removes the whole request.",
    "how": [
      "Check cheap synchronous conditions first, before the await.",
      "Start shared async work early only when every branch needs it.",
      "Keep the original order when the local check is expensive or depends on the fetched value."
    ],
    "bullets": [
      "Await: pause until a promise settles. [Definition](glossary.md)"
    ],
    "verify": "The network tab shows zero requests when the local check fails.",
    "links": [
      {
        "kind": "docs",
        "label": "Vercel: check cheap conditions before async flags",
        "url": "https://github.com/vercel-labs/agent-skills/tree/main/skills/react-best-practices/rules/async-cheap-condition-before-await.md"
      }
    ],
    "smellCode": "const flag = await getFlag();\nif (flag && localCheck) { ... }",
    "example": "// Bad: the request runs even when localCheck is false.\nconst flag = await getFlag();\nif (flag && localCheck) {\n  loadFeature();\n}\n\n// Good: the request runs only when every local check passed.\nif (localCheck) {\n  const flag = await getFlag();\n  if (flag) {\n    loadFeature();\n  }\n}"
  },
  {
    "id": "M-01",
    "title": "Core Web Vitals",
    "severity": "Medium",
    "category": "metrics",
    "smell": false,
    "summary": "No field data and no budgets for LCP, INP, CLS.",
    "why": "LCP is Largest Contentful Paint, when the main content appears. INP is Interaction to Next Paint, how fast the page answers a click. CLS is Cumulative Layout Shift, how much the page jumps.",
    "how": [
      "Track with web-vitals.",
      "Set budgets in Lighthouse CI."
    ],
    "bullets": [
      "LCP: Largest Contentful Paint. When the main content appears. Under 2.5 s.",
      "INP: Interaction to Next Paint. How fast the page answers a click. Under 200 ms.",
      "CLS: Cumulative Layout Shift. How much the page jumps. Under 0.1."
    ],
    "verify": "Budgets pass on every push.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev vitals",
        "url": "https://web.dev/articles/vitals"
      },
      {
        "kind": "docs",
        "label": "web-vitals package",
        "url": "https://www.npmjs.com/package/web-vitals"
      },
      {
        "kind": "docs",
        "label": "Lighthouse CI",
        "url": "https://github.com/GoogleChrome/lighthouse-ci"
      }
    ]
  },
  {
    "id": "M-02",
    "title": "Long tasks",
    "severity": "Medium",
    "category": "metrics",
    "smell": false,
    "summary": "Work on the main thread that blocks the screen.",
    "why": "An interaction is a click, a keypress, or a scroll. A handler is the function that runs when it happens. Blocking time is the time the page cannot respond because the main thread is busy.",
    "how": [
      "Open the Chrome Performance panel, record an interaction, read the blocking time.",
      "Move the work out of the handler. Defer what is not urgent."
    ],
    "bullets": [
      "Long task: work on the main thread that blocks the screen for a moment. [Definition](glossary.md)",
      "Interaction: a click, a keypress, a scroll.",
      "Handler: the function that runs when the interaction happens.",
      "Blocking time: the time the page cannot respond because the main thread is busy.",
      "Example: a click handler that loops over 100,000 items blocks the page. Move the loop to a worker, or chunk it."
    ],
    "verify": "The blocking time drops, and INP follows.",
    "links": [
      {
        "kind": "docs",
        "label": "Optimize long tasks",
        "url": "https://web.dev/articles/optimize-long-tasks"
      }
    ],
    "example": "// Bad: the click handler blocks the page.\nfunction onClick() {\n  const rows = [];\n  for (let i = 0; i < 100000; i++) {\n    rows.push(heavyWork(i));\n  }\n  setRows(rows);\n}\n\n// Good: chunk the work so the page stays responsive.\nfunction onClick() {\n  const rows = [];\n  let i = 0;\n  function nextChunk() {\n    const end = Math.min(i + 1000, 100000);\n    for (; i < end; i++) {\n      rows.push(heavyWork(i));\n    }\n    setRows([...rows]);\n    if (i < 100000) {\n      setTimeout(nextChunk, 0);\n    }\n  }\n  nextChunk();\n}"
  },
  {
    "id": "M-03",
    "title": "Field data over lab scores",
    "severity": "Medium",
    "category": "metrics",
    "smell": false,
    "summary": "Decisions based only on lab scores, or a lab score gamed for its own sake.",
    "why": "Lab scores come from one controlled machine. Real users are on other devices and networks. A 99 lab score can still feel slow to real users, and over-optimizing for the lab can hurt the real page.",
    "how": [
      "Collect field data with web-vitals and send it to your analytics.",
      "Check CrUX for the URL or origin to see real-user distributions.",
      "Set CI thresholds slightly below production targets; CI runners score 10-20 points lower."
    ],
    "bullets": [
      "Lab data: measured on one controlled machine. [Definition](glossary.md)",
      "Field data: measured by real users in production. [Definition](glossary.md)",
      "CrUX: Google's real-user data for a URL or origin. [Definition](glossary.md)"
    ],
    "verify": "A budget change is justified by field data, not just by the lab score.",
    "links": [
      {
        "kind": "docs",
        "label": "CrUX",
        "url": "https://developer.chrome.com/docs/crux"
      },
      {
        "kind": "docs",
        "label": "web-vitals",
        "url": "https://www.npmjs.com/package/web-vitals"
      }
    ]
  },
  {
    "id": "M-04",
    "title": "Effect cleanup and memory",
    "severity": "Medium",
    "category": "metrics",
    "smell": false,
    "summary": "Listeners, timers, and subscriptions that outlive their component.",
    "why": "A component that unmounts while a listener or interval keeps running leaks work and memory. The leak grows with every mount.",
    "how": [
      "Return a cleanup function from every effect that subscribes, listens, or schedules.",
      "Clear intervals and timeouts in the cleanup.",
      "Remove window and document listeners in the cleanup.",
      "Check with heap snapshots: a stable page should not grow memory across mounts."
    ],
    "bullets": [
      "Cleanup: the function an effect returns, run before the next effect or unmount. [Definition](glossary.md)"
    ],
    "verify": "Two heap snapshots of the same page show no growth.",
    "links": [
      {
        "kind": "docs",
        "label": "React docs: effects with cleanup",
        "url": "https://react.dev/learn/synchronizing-with-effects#effects-with-cleanup"
      }
    ]
  },
  {
    "id": "G-01",
    "title": "Lint with autofix",
    "severity": "High",
    "category": "guardrails",
    "smell": false,
    "summary": "Broken hook rules and dependency arrays are invisible while typing.",
    "why": "A linter checks the code as you write it. Autofix applies the fix for you.",
    "how": [
      "Turn on eslint-plugin-react-hooks and eslint-plugin-react-compiler.",
      "Config included: eslint.config.js."
    ],
    "bullets": [
      "How to run without pushing: run it locally. CI is the same commands, automated."
    ],
    "verify": "Run npm run lint. Zero errors means it is on.",
    "links": [
      {
        "kind": "docs",
        "label": "eslint-plugin-react-hooks",
        "url": "https://www.npmjs.com/package/eslint-plugin-react-hooks"
      },
      {
        "kind": "docs",
        "label": "eslint-plugin-react-compiler",
        "url": "https://www.npmjs.com/package/eslint-plugin-react-compiler"
      }
    ],
    "example": "// eslint.config.js\nimport reactHooks from \"eslint-plugin-react-hooks\";\nimport reactCompiler from \"eslint-plugin-react-compiler\";\n\nexport default [\n  {\n    plugins: {\n      \"react-hooks\": reactHooks,\n      \"react-compiler\": reactCompiler,\n    },\n    rules: {\n      \"react-hooks/rules-of-hooks\": \"error\",\n      \"react-hooks/exhaustive-deps\": \"error\",\n      \"react-compiler/react-compiler\": \"error\",\n    },\n  },\n];",
    "exampleLang": "js"
  },
  {
    "id": "G-02",
    "title": "React Compiler",
    "severity": "Medium",
    "category": "guardrails",
    "smell": false,
    "summary": "Manual memoization is error-prone and noisy.",
    "why": "React Compiler adds memoization at build time. Fewer re-renders without manual memo code.",
    "how": [
      "Enable babel-plugin-react-compiler on React 19."
    ],
    "bullets": [],
    "verify": "The Profiler shows fewer re-renders with no manual memo code.",
    "links": [
      {
        "kind": "docs",
        "label": "React Compiler docs",
        "url": "https://react.dev/learn/react-compiler"
      }
    ]
  },
  {
    "id": "G-03",
    "title": "Perf tests",
    "severity": "Medium",
    "category": "guardrails",
    "smell": false,
    "summary": "Perf fixes silently regress later.",
    "why": "A test that fails when a component re-renders for no reason stops the regression before it ships.",
    "how": [
      "Write a call-count test. Example: test/perf-regression.test.jsx.",
      "Use React.Profiler for duration asserts."
    ],
    "bullets": [
      "How to run without pushing: `npm test`."
    ],
    "verify": "The test goes red on a regression, green after the fix.",
    "links": [
      {
        "kind": "docs",
        "label": "React.Profiler docs",
        "url": "https://react.dev/reference/react/Profiler"
      }
    ],
    "example": "// test/perf-regression.test.jsx\nimport { fireEvent, render } from \"@testing-library/react\";\nimport { memo, useCallback, useState } from \"react\";\nimport { expect, it, vi } from \"vitest\";\n\nconst renderRow = vi.fn(({ onClick }) => (\n  <button onClick={onClick}>bump</button>\n));\nconst Row = memo(renderRow);\n\nit(\"keeps the memoized row still when the callback is stable\", () => {\n  function Parent() {\n    const [, setCount] = useState(0);\n    const bump = useCallback(() => setCount((c) => c + 1), []);\n    return <Row onClick={bump} />;\n  }\n\n  render(<Parent />);\n  fireEvent.click(document.querySelector(\"button\"));\n  expect(renderRow).toHaveBeenCalledTimes(1);\n});"
  },
  {
    "id": "G-04",
    "title": "CI gates",
    "severity": "Medium",
    "category": "guardrails",
    "smell": false,
    "summary": "A slow change can merge without anyone noticing.",
    "why": "CI is checks that run on every push. It is the same commands you run locally, automated.",
    "how": [
      "Run lint and tests.",
      "Add Lighthouse CI budgets.",
      "Add React Doctor as a required check."
    ],
    "bullets": [
      "CI: checks that run on every push. [Definition](glossary.md)",
      "How to run without pushing: run the same commands locally. CI is just automation of them."
    ],
    "verify": "A slow change cannot merge.",
    "links": [
      {
        "kind": "docs",
        "label": "React Doctor CI",
        "url": "https://react.doctor/ci"
      }
    ]
  },
  {
    "id": "F-01",
    "title": "Image formats and sizes",
    "severity": "Medium",
    "category": "frontend",
    "smell": true,
    "smellText": "Images served too big for the screen",
    "summary": "Images in the wrong format, or one size for every screen.",
    "why": "A 2000px JPEG for a 300px slot costs every user download time. WebP or AVIF is smaller, and srcset serves the right size per screen.",
    "how": [
      "Serve WebP or AVIF instead of JPEG or PNG where the format is supported.",
      "Add srcset and sizes so small screens download small images.",
      "Compress the source image. Aim for the smallest file that still looks right."
    ],
    "bullets": [
      "srcset: a list of image sizes. The browser picks the one that fits the screen. [Definition](glossary.md)"
    ],
    "verify": "Lighthouse image audits pass, and the page downloads fewer bytes.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev image guidance",
        "url": "https://web.dev/learn/images"
      }
    ],
    "smellCode": "<img src=\"hero.jpg\" />",
    "example": "// Bad: one huge image for every screen.\n<img src=\"hero.jpg\" />\n\n// Good: the browser picks the size that fits.\n<img\n  src=\"hero-800.webp\"\n  srcset=\"hero-400.webp 400w, hero-800.webp 800w, hero-1600.webp 1600w\"\n  sizes=\"(max-width: 600px) 100vw, 50vw\"\n/>"
  },
  {
    "id": "F-02",
    "title": "Font delivery",
    "severity": "Medium",
    "category": "frontend",
    "smell": false,
    "summary": "Fonts that block text, or load more glyphs than the page needs.",
    "why": "A font that blocks first paint delays the text users came to read. font-display: swap shows fallback text first, and subsetting ships fewer bytes.",
    "how": [
      "Use font-display: swap so text shows while the font loads.",
      "Preload the font file the page needs first.",
      "Subset the font, or use a variable font, so you ship only the glyphs you use."
    ],
    "bullets": [
      "font-display: swap: show fallback text immediately, swap in the real font when it loads. [Definition](glossary.md)"
    ],
    "verify": "Lighthouse font audits pass, and text appears before the font finishes loading.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev font best practices",
        "url": "https://web.dev/articles/font-best-practices"
      }
    ],
    "example": "/* Bad: text is invisible until the font loads. */\n@font-face {\n  font-family: \"Body\";\n  src: url(\"/fonts/body.woff2\");\n}\n\n/* Good: fallback text shows first, then the real font swaps in. */\n@font-face {\n  font-family: \"Body\";\n  src: url(\"/fonts/body.woff2\");\n  font-display: swap;\n}"
  },
  {
    "id": "F-03",
    "title": "CSS delivery",
    "severity": "Medium",
    "category": "frontend",
    "smell": false,
    "summary": "Render-blocking CSS, or CSS the page never uses.",
    "why": "CSS blocks first paint. A big stylesheet delays the first useful frame, and unused rules cost download time for nothing.",
    "how": [
      "Inline the critical CSS for the first screen.",
      "Load the rest of the CSS asynchronously.",
      "Purge unused CSS in the build."
    ],
    "bullets": [
      "Critical CSS: the styles the first screen needs, inlined so the page paints without waiting. [Definition](glossary.md)"
    ],
    "verify": "Lighthouse render-blocking audit passes, and the first paint is faster.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev render-blocking resources",
        "url": "https://web.dev/articles/render-blocking-resources"
      }
    ],
    "example": "<!-- Bad: the whole stylesheet blocks first paint. -->\n<link rel=\"stylesheet\" href=\"/app.css\" />\n\n<!-- Good: critical styles inline, the rest loads after. -->\n<style>/* critical styles for the first screen */</style>\n<link rel=\"stylesheet\" href=\"/app.css\" media=\"print\" onload=\"this.media='all'\" />"
  },
  {
    "id": "F-04",
    "title": "Delivery: caching and compression",
    "severity": "Medium",
    "category": "frontend",
    "smell": false,
    "summary": "No compression, no cache headers, or no CDN.",
    "why": "Compression shrinks what travels over the network. Cache headers stop repeat visits from downloading the same bytes. A CDN puts the bytes near the user.",
    "how": [
      "Turn on gzip or brotli compression.",
      "Set cache-control headers. Long cache for hashed assets, short for HTML.",
      "Serve static assets from a CDN."
    ],
    "bullets": [
      "CDN: a network of servers that serves files from the one nearest the user. [Definition](glossary.md)"
    ],
    "verify": "Lighthouse network audits pass, and repeat visits download fewer bytes.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev caching guidance",
        "url": "https://web.dev/articles/http-cache"
      }
    ],
    "example": "// Bad: every visit downloads the same bytes.\nCache-Control: no-store\n\n// Good: hashed assets stay cached for a year.\nCache-Control: public, max-age=31536000, immutable"
  },
  {
    "id": "F-05",
    "title": "Third-party scripts",
    "severity": "Medium",
    "category": "frontend",
    "smell": true,
    "smellText": "Heavy third-party scripts on every page",
    "summary": "Heavy third-party scripts that block the main thread.",
    "why": "Analytics, chat widgets, and ad scripts run on the main thread. Each one competes with your app for the user's attention.",
    "how": [
      "Load third-party scripts with defer or async so they do not block first paint.",
      "Load them only on the pages that need them.",
      "Remove the ones you do not use. Self-host the ones you keep."
    ],
    "bullets": [
      "defer: download in the background, run after the page parses. [Definition](glossary.md)"
    ],
    "verify": "Lighthouse third-party audit passes, and the main thread is quieter.",
    "links": [
      {
        "kind": "docs",
        "label": "web.dev third-party guidance",
        "url": "https://web.dev/articles/third-party-javascript"
      },
      {
        "kind": "docs",
        "label": "Partytown",
        "url": "https://github.com/QwikDev/partytown"
      }
    ],
    "smellCode": "<script src=\"https://analytics.example.com/tracker.js\"></script>",
    "example": "<!-- Bad: blocks the page while it downloads and runs. -->\n<script src=\"https://analytics.example.com/tracker.js\"></script>\n\n<!-- Good: downloads in the background, runs after the page parses. -->\n<script defer src=\"https://analytics.example.com/tracker.js\"></script>"
  },
  {
    "id": "S-01",
    "title": "Store selectors",
    "severity": "Medium",
    "category": "state",
    "smell": true,
    "smellText": "Subscribing to more state than the component uses",
    "summary": "A component re-renders on every store change when it subscribes without a selector.",
    "why": "The store notifies every subscriber on every change. A selector narrows the subscription to the slice the component uses, and many stores memoize the result so unchanged slices do not re-render.",
    "how": [
      "Zustand: useStore((state) => state.count), not useStore().",
      "Redux: useSelector with the exact slice, not the whole state.",
      "Subscribe to derived booleans, not raw values: isCartEmpty = (s) => s.cart.length === 0, not s.cart."
    ],
    "bullets": [
      "Selector: a function that picks the slice of state a component reads. [Definition](glossary.md)"
    ],
    "verify": "The Profiler shows one re-render per slice change, not per store change.",
    "links": [
      {
        "kind": "docs",
        "label": "Zustand selectors",
        "url": "https://zustand.docs.pmnd.rs/guides/using-slices"
      },
      {
        "kind": "docs",
        "label": "Redux useSelector",
        "url": "https://react-redux.js.org/api/hooks#useselector"
      }
    ],
    "smellCode": "const state = useStore();",
    "example": "// Bad: subscribes to the whole store. Any change re-renders.\nconst count = useStore();\n\n// Good: subscribes to one slice. Other changes do not re-render.\nconst count = useStore((state) => state.count);"
  },
  {
    "id": "S-02",
    "title": "Derive during render, not in effects",
    "severity": "Medium",
    "category": "state",
    "smell": true,
    "smellText": "Derived state computed in useEffect",
    "summary": "Computing a value in an effect means a wasted render and an extra render.",
    "why": "The effect runs after the paint, updates state, and paints again. Computing the same value during render gives the right answer on the first paint with zero extra renders.",
    "how": [
      "Compute derived values in the render body, or with useMemo for expensive work.",
      "Adjust state during render with a setState call of the adjust-when-rendering pattern only when a prop changed and you must keep it in sync.",
      "Do not fetch-then-set derived state in an effect when the value can be computed."
    ],
    "verify": "The Profiler shows one render instead of two for the same change.",
    "links": [
      {
        "kind": "docs",
        "label": "React docs: you might not need an effect",
        "url": "https://react.dev/learn/you-might-not-need-an-effect"
      }
    ],
    "smellCode": "const [fullName, setFullName] = useState(\"\");\nuseEffect(() => setFullName(`${first} ${last}`), [first, last]);",
    "example": "// Bad: an effect updates state after the first paint.\nuseEffect(() => setFullName(`${first} ${last}`), [first, last]);\n\n// Good: computed on the first paint, no effect needed.\nconst fullName = `${first} ${last}`;"
  },
  {
    "id": "S-03",
    "title": "Lazy state initialization",
    "severity": "Medium",
    "category": "state",
    "smell": false,
    "summary": "Expensive initializer work runs on every render.",
    "why": "useState(expression) evaluates the expression on every render even though the result is used only on the first. useState(() => expression) runs it once.",
    "how": [
      "Pass a function to useState when the initial value is expensive: JSON.parse, localStorage reads, or a big computation.",
      "The function runs once, on first render."
    ],
    "verify": "The expensive code runs once per mount, not per render.",
    "links": [
      {
        "kind": "docs",
        "label": "React docs: lazy initial state",
        "url": "https://react.dev/reference/react/useState#avoiding-recreating-the-initial-state"
      }
    ],
    "example": "// Bad: parses localStorage on every render.\nconst [config] = useState(JSON.parse(localStorage.getItem(\"config\")));\n\n// Good: parses once, on first render.\nconst [config] = useState(() => JSON.parse(localStorage.getItem(\"config\")));"
  },
  {
    "id": "S-04",
    "title": "Transitions for non-urgent updates",
    "severity": "Medium",
    "category": "state",
    "smell": false,
    "summary": "Expensive updates block input because they all render at full priority.",
    "why": "The browser must finish a render before answering the next interaction. An expensive update like filtering a large list blocks typing. startTransition marks the update as non-urgent so React keeps the UI responsive and shows the old value until the new one is ready.",
    "how": [
      "Wrap the urgent-then-expensive pattern: keep the input update urgent, wrap the list update in startTransition.",
      "useDeferredValue for the value that drives the expensive render.",
      "Show the pending state from useTransition so the UI explains the delay."
    ],
    "bullets": [
      "startTransition: mark an update as non-urgent. [Definition](glossary.md)",
      "useDeferredValue: defer a value, not a whole update. [Definition](glossary.md)"
    ],
    "verify": "Typing stays responsive while the filtered list catches up. INP improves.",
    "links": [
      {
        "kind": "docs",
        "label": "React docs: startTransition",
        "url": "https://react.dev/reference/react/startTransition"
      },
      {
        "kind": "docs",
        "label": "React docs: useDeferredValue",
        "url": "https://react.dev/reference/react/useDeferredValue"
      }
    ],
    "example": "// Bad: the list update has the same priority as the keystroke.\n<input value={query} onChange={(e) => {\n  setQuery(e.target.value);\n  setFiltered(filter(e.target.value));\n}} />\n\n// Good: the keystroke stays urgent, the list render does not.\nconst [isPending, startTransition] = useTransition();\n<input value={query} onChange={(e) => {\n  setQuery(e.target.value);\n  startTransition(() => setFiltered(filter(e.target.value)));\n}} />"
  }
]
